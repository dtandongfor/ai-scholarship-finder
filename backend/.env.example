# Comma-separated browser origins permitted to call the production API.
# Replace this example with the real deployed frontend address.
INTELLIBLE_ALLOWED_ORIGINS=https://your-intellible-domain.example
